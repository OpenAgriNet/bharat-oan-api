# Append these to your .env for local MCP + API testing

MCP_SERVER_URL=http://127.0.0.1:3001/mcp
MCP_TIMEOUT_SECONDS=120
# MCP_API_KEY=          # only if MCP server enforces auth

ENVIRONMENT=local

# Redis (default localhost:6379)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379