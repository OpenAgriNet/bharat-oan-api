## basic setup packages
wheel==0.47.0
setuptools==83.0.0

httpx_oauth==0.17.0
Jinja2==3.1.6 # For template rendering

# for data/DB
pandas==3.0.5
simplejson==4.1.1

### for DB
# requests  # Replaced with httpx

# LLM
openai==2.48.0
pydantic==2.13.4
tenacity==9.1.4

# Logfire
# logfire
gevent==26.7.0

# Redis for caching and stuff
redis==8.0.1
# NOTE: not sure if needed
# hiredis-py 

# Settings
python-decouple==3.8

openpyxl==3.1.5

### Pydantic AI (Updated to resolve conflicts)
# 1.2.1 through 1.31.0 unconditionally do `from opentelemetry._events import
# Event` in pydantic_ai/messages.py, which no released opentelemetry-api
# version actually provides -- ModuleNotFoundError at import time, on any
# opentelemetry-api version. Fixed upstream in pydantic-ai-slim 1.32.0.
pydantic-ai==1.32.0
asgiref==3.12.1

# Web Server
gunicorn==26.0.0
uvicorn[standard]==0.34.3  # ASGI server
uvicorn-worker==0.3.0

# FastAPI and dependencies
fastapi[standard]==0.140.0
starlette==1.3.1
httpx==0.28.1

# Async HTTP client for SATHI API optimization
# aiohttp

# Search and Data
marqo==3.18.2 # Vector search
qdrant-client==1.18.0 # Qdrant vector search (scheme documents)
sentence-transformers==5.6.1 # Embeddings for Qdrant scheme search
rapidfuzz==3.14.5 # Fuzzy string matching

# Cloud Services
boto3==1.43.56 # AWS SDK
google-cloud-translate==3.27.0 # Google Cloud Translation API
google-auth==2.56.2 # OAuth2 service account credentials (Play Integrity verification)

# Authentication and Security
PyJWT==2.13.0
cryptography==49.0.0

# Async utilities
aiocache==0.12.3
pydantic-settings==2.9.1
python-dateutil==2.9.0.post0
pytz==2026.3.post1 # Timezone support

langcodes[data]==3.4.0

# python dotenv
python-dotenv==1.2.2

markdownify==1.2.3

# Human-friendly formatting
humanize==4.16.0

pycryptodome==3.23.0

langfuse==4.14.1
